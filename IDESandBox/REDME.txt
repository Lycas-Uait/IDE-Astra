IDE SandBox - Простая, быстрая и лёгкая IDE для написания и запуска кода на C++, без установки тяжёлых программ вроде Visual Studio.
Работает в консоли, полностью оффлайн, поддерживает редактирование текста, запуск кода и вывод результата.

Возможности:
Подсветка курсора при редактировании кода
Управление стрелками (← ↑ ↓ →)
Поддержка переноса строк (Enter) и удаления (Backspace)
Запуск кода сочетанием клавиш Ctrl + S
Автоматическая компиляция и запуск программы
Выход из IDE командой :exit
Автоматическое создание папки projects для временных файлов.

Как использовать:
Запусти файл C++.exe
Пиши код на C++ прямо в окне IDE
Нажми Ctrl + S, чтобы скомпилировать и запустить
Ошибки компиляции (если есть) появятся в консоли
Для выхода напиши :exit
Для работы IDE нужен установленный MinGW (g++) — компилятор языка C++.

Установка MinGW:

В папке IDE находится установщик mingw-w64.exe (или msys2.exe).
Если его нет — скачай с официального сайта:
👉 https://www.mingw-w64.org/downloads/
Установи MinGW в путь без пробелов, например:
C:\MinGW
Добавь MinGW в системную переменную PATH:
Нажми Win + R, введи sysdm.cpl
Перейди во вкладку Дополнительно → Переменные среды
Найди переменную Path → Изменить
Добавь новую строку:
C:\MinGW\bin
Сохрани и перезапусти IDE
Проверить установку:
Открой командную строку (Win + R → cmd)
Введи:
g++ --version
Если появится версия компилятора — всё готово.


Советы:

Не используй пробелы или символ + в названии файлов.
Если IDE пишет, что g++ не найден — значит PATH не настроен.
Всё, что ты пишешь, сохраняется в projects/temp.cpp.
После успешной компиляции запускается projects/temp.exe.